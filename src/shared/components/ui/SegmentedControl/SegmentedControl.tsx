import * as Mantine from '@mantine/core';

type SegmentedControlProps = Mantine.SegmentedControlProps;

export const SegmentedControl = (props: SegmentedControlProps) => (
  <Mantine.SegmentedControl {...props} />
);
